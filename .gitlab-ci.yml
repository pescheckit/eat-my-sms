stages:
  - build

build-deb:
  image: debian
  stage: build
  script:
    - apt update && apt install -y debhelper-compat config-package-dev
    - dpkg-buildpackage -uc -us
    - mv ../*.deb ./
  artifacts:
    paths:
      - "*.deb"
